import React from "react";
import OptionPage from "@/theme/templates/OptionPage";
import {PageParams} from "@/types";

export default function Page(props: PageParams) {
  return <OptionPage {...props}/>
}
