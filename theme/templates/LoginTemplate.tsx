'use client'

import {Container} from "@mui/material";
import LoginForm from "@/theme/sections/LoginForm";

export default function LoginTemplate() {
  return <Container maxWidth="sm" style={{padding: '20px'}}>

    <LoginForm/>
  </Container>
}
